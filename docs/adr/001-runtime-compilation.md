# ADR-001：运行时编译作为主路径

- 状态：Accepted
- 日期：2026-08-05

## 决策

YAML 经 DSL 和 IR 校验后，直接调用 LangGraph 公共 API 构图。Python 源码生成仅作为未来的
调试、迁移或脚手架能力，不是执行前提。

## 后果

避免模板漂移和执行生成代码的安全风险；需要集中维护 LangGraph 兼容适配器和行为契约测试。

